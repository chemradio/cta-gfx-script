function Parameters(compWidth, compHeight, compLength, compFrameRate) {
    this.compWidth = compWidth;
    this.compHeight = compHeight;
    this.compLength = compLength;
    this.compFrameRate = compFrameRate;
}